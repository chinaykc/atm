// Package cli implements the ATM command-line application.
//
// The package is intentionally thin: it owns argument parsing, command
// composition, terminal output, and exit-code mapping. Language compilation,
// runtime execution, storage, and integrations live in lower-level packages.
package cli
