/for 3 until tests pass
Fix the implementation until the test suite passes.

/for 5 until documentation is accurate and complete
Update the README and command reference to match the current behavior.

/for until(exists("release-gate.json") && json("release-gate.json").passed)
Keep resolving release blockers until release-gate.json reports passed=true.

/for 2
Run a focused review pass on the code and tests.
