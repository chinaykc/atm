/doc Shared definitions imported by the complex smoke examples. Definitions return fixed, easy-to-check values.

/def tag name
/return smoke:{{name}}

/def pair key value
/return
```
{{key}}={{value}}
```
