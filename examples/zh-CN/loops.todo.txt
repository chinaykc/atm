/for 3 until 测试通过
修复实现，直到测试套件通过。

/for 5 until 文档准确且完整
更新 README 和命令参考，使其与当前行为一致。

/for until(exists("release-gate.json") && json("release-gate.json").passed)
持续解决发布阻塞，直到 release-gate.json 中 passed=true。

/for 2
对代码和测试做一次聚焦审查。
